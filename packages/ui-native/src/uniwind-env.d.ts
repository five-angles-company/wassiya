// Brings Uniwind's `className` prop augmentation for React Native components
// into this package so it typechecks in isolation. The consuming app provides
// `uniwind` at runtime (it is a peer dependency here).
/// <reference types="uniwind/types" />

export {}
