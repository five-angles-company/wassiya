import { Stack } from "expo-router"

/** Section ٦ sub-screens. The Centre itself is the tab; these are its pages. */
export default function ProtectionLayout() {
  return <Stack screenOptions={{ headerShown: false }} />
}
